#include <stdio.h>

int main() {
// declaração de variaveis
// Entrada
int nota1 = 8;
int nota2 = 7;

// Pocessamento
int media = (nota1 + nota2) / 2;

// Saida
printf("a media e %d", media);
   
return 0;
}